{
  "name": "@citizenry/migrator",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/build-migrations.mjs",
    "predeploy": "node scripts/build-migrations.mjs",
    "predev": "node scripts/build-migrations.mjs",
    "pretypecheck": "node scripts/build-migrations.mjs",
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260516.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
