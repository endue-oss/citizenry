{
  "name": "citizenry",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @citizenry/spec run build && pnpm --filter './apps/*' --parallel run dev",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm --filter @citizenry/spec run build && pnpm -r run typecheck",
    "test": "pnpm --filter @citizenry/spec run build && pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
